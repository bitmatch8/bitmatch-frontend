export enum RefreshConfig {
  detail_refreshInterval = 600000,
  publicInfo_refreshInterval = 15000,
  whiteInfo_refreshInterval = 15000,
  history_refreshInterval = 30000,
  balance_refreshInterval = 60000,
  network_refreshInterval = 5000,
  submit_order_refreshInterval = 60000,
  fees_refreshInterval = 60000,
  rune_refreshInterval = 30000,
}

export enum VirtualBytesConfig {
  NFT = 141,
  FT = 153,
}
